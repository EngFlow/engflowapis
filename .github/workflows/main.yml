name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  presubmit-checks:
    runs-on: [ self-hosted, ubuntu2004, x64 ]

    steps:
    - uses: actions/checkout@v2

    - name: Check formatting
      run: bazel build //...
      shell: bash
